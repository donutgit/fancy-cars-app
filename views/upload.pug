extends layout

block content
  h1 Upload image
  form(
      ref='uploadForm' 
      id='uploadForm' 
      action='/upload' 
      method='post' 
      encType="multipart/form-data"
  )
    input( type="file" name="sampleFile")
    input( type='submit' value='Upload!')
